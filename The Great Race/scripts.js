
//initializing racer objects 
const racer1 = {
  initialPosition: 0,
  position: 0,
  minSpeed: 2,
  maxSpeed: 4,
}

const racer2 = {
  initialPosition: 0,
  position: 0,
  minSpeed: 2,
  maxSpeed: 4,
}

const racer3 = {
  initialPosition: 0,
  position: 0,
  minSpeed: 2,
  maxSpeed: 4,
}

const racer4 = {
  initialPosition: 0,
  position: 0,
  minSpeed: 2,
  maxSpeed: 4,
}

//getting initial positions of racers
var x = document.getElementById('racer1');
racer1['initialPosition'] = x.offsetLeft
racer1['position'] = x.offsetLeft
racer2['initialPosition'] = x.offsetLeft
racer2['position'] = x.offsetLeft
racer3['initialPosition'] = x.offsetLeft
racer3['position'] = x.offsetLeft
racer4['initialPosition'] = x.offsetLeft
racer4['position'] = x.offsetLeft